<x-app-layout>
    <x-slot name="header"><div><p class="casa-section-label">{{ __('Commission detail') }}</p><h1 class="mt-2 font-display text-3xl font-black text-casa-text">PHP {{ number_format((float) $commission->commission_amount, 2) }}</h1><p class="mt-2 text-sm text-casa-muted">{{ ucfirst($commission->commission_type) }} · {{ ucfirst($commission->status) }}</p></div><a href="{{ route('staff.commissions.index') }}" class="casa-button-secondary">{{ __('My commissions') }}</a></x-slot>
    <x-app-card><dl class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3"><div><dt class="casa-section-label">{{ __('Service') }}</dt><dd class="mt-2 font-semibold">{{ $commission->appointment?->service?->name }}</dd></div><div><dt class="casa-section-label">{{ __('Appointment') }}</dt><dd class="mt-2">{{ $commission->appointment?->appointment_number }}</dd></div><div><dt class="casa-section-label">{{ __('Transaction') }}</dt><dd class="mt-2">{{ $commission->transaction?->transaction_number }}</dd></div><div><dt class="casa-section-label">{{ __('Basis') }}</dt><dd class="mt-2">PHP {{ number_format((float) $commission->basis_amount, 2) }}</dd></div><div><dt class="casa-section-label">{{ __('Rate') }}</dt><dd class="mt-2">{{ number_format((float) $commission->commission_rate * 100, 0) }}%</dd></div><div><dt class="casa-section-label">{{ __('Earned date') }}</dt><dd class="mt-2">{{ $commission->earned_at?->format('M d, Y g:i A') }}</dd></div><div><dt class="casa-section-label">{{ __('Payout date') }}</dt><dd class="mt-2">{{ $commission->paid_at?->format('M d, Y') ?: __('Pending') }}</dd></div></dl></x-app-card>
</x-app-layout>
