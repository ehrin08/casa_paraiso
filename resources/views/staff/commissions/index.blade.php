<x-app-layout>
    <x-slot name="header"><div><p class="casa-section-label">{{ __('Therapist workspace') }}</p><h1 class="mt-2 font-display text-3xl font-black text-casa-text">{{ __('My commissions') }}</h1><p class="mt-2 text-sm text-casa-muted">{{ __('Review earnings and externally recorded payouts.') }}</p></div></x-slot>
    <div class="space-y-5"><section class="casa-metric-grid grid gap-3 md:grid-cols-3" data-metric-grid><x-metric-card label="Pending balance" value="PHP {{ number_format((float) $totals['pending'], 2) }}" meta="Awaiting settlement" tone="gold"/><x-metric-card label="Paid total" value="PHP {{ number_format((float) $totals['paid'], 2) }}" meta="Recorded payouts" tone="green"/><x-metric-card label="Net commission" value="PHP {{ number_format((float) $totals['net'], 2) }}" meta="Earnings and adjustments" tone="brown"/></section><x-app-card>@if($commissions->isEmpty())<x-empty-state title="{{ __('No commissions yet') }}" description="{{ __('Fully paid completed services will appear here.') }}"/>@else<x-table-shell label="{{ __('My commission records') }}"><thead class="bg-casa-bg text-left text-xs font-black uppercase tracking-[0.1em] text-casa-muted"><tr><th class="px-4 py-3">{{ __('Service') }}</th><th class="px-4 py-3">{{ __('Transaction') }}</th><th class="px-4 py-3">{{ __('Basis') }}</th><th class="px-4 py-3">{{ __('Rate') }}</th><th class="px-4 py-3">{{ __('Commission') }}</th><th class="px-4 py-3">{{ __('Earned') }}</th><th class="px-4 py-3">{{ __('Status') }}</th><th class="px-4 py-3">{{ __('Payout') }}</th><th class="px-4 py-3">{{ __('Action') }}</th></tr></thead><tbody class="divide-y divide-casa-border text-sm">@foreach($commissions as $commission)<tr class="casa-table-row"><td class="px-4 py-4">{{ $commission->appointment?->service?->name }}</td><td class="px-4 py-4 text-casa-muted">{{ $commission->transaction?->transaction_number }}</td><td class="px-4 py-4">PHP {{ number_format((float) $commission->basis_amount, 2) }}</td><td class="px-4 py-4">{{ number_format((float) $commission->commission_rate * 100, 0) }}%</td><td class="px-4 py-4 font-semibold">PHP {{ number_format((float) $commission->commission_amount, 2) }}</td><td class="px-4 py-4 text-casa-muted">{{ $commission->earned_at?->format('M d, Y') }}</td><td class="px-4 py-4"><x-status-badge>{{ ucfirst($commission->status) }}</x-status-badge></td><td class="px-4 py-4 text-casa-muted">{{ $commission->paid_at?->format('M d, Y') ?: '—' }}</td><td class="px-4 py-4"><a class="font-bold text-casa-primary" href="{{ route('staff.commissions.show', $commission) }}">{{ __('Open') }}</a></td></tr>@endforeach</tbody></x-table-shell><div class="mt-5">{{ $commissions->links() }}</div>@endif</x-app-card></div>
</x-app-layout>
