<x-app-layout><x-slot name="header"><div><p class="casa-section-label">{{ __('Payment detail') }}</p><h1 class="mt-2 font-display text-3xl font-black text-casa-text">{{ $transaction->transaction_number }}</h1><p class="mt-2 text-sm text-casa-muted">{{ $transaction->customerProfile?->user?->name }}</p></div><div class="flex gap-2"><a href="{{ route('reception.transactions.edit',$transaction) }}" class="casa-button-primary">{{ __('Edit') }}</a><a href="{{ route('reception.transactions.index') }}" class="casa-button-secondary">{{ __('Payments') }}</a></div></x-slot><x-app-card><dl class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3"><div><dt class="casa-section-label">{{ __('Amount') }}</dt><dd class="mt-2 text-2xl font-black">PHP {{ number_format((float)$transaction->amount,2) }}</dd></div><div><dt class="casa-section-label">{{ __('Status') }}</dt><dd class="mt-2"><x-status-badge>{{ ucfirst($transaction->payment_status) }}</x-status-badge></dd></div><div><dt class="casa-section-label">{{ __('Method') }}</dt><dd class="mt-2">{{ $transaction->payment_method ?: __('Not set') }}</dd></div><div><dt class="casa-section-label">{{ __('Service') }}</dt><dd class="mt-2">{{ $transaction->service?->name }}</dd></div><div><dt class="casa-section-label">{{ __('Paid at') }}</dt><dd class="mt-2">{{ $transaction->paid_at?->format('M d, Y g:i A') ?: __('Not paid') }}</dd></div><div><dt class="casa-section-label">{{ __('Recorded by') }}</dt><dd class="mt-2">{{ $transaction->recorder?->name }}</dd></div></dl></x-app-card></x-app-layout>
