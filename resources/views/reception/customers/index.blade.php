<x-app-layout><x-slot name="header"><div><p class="casa-section-label">{{ __('Reception customers') }}</p><h1 class="mt-2 font-display text-3xl font-black text-casa-text">{{ __('Customers') }}</h1><p class="mt-2 text-sm text-casa-muted">{{ __('Find contact details, bookings, and payment history.') }}</p></div></x-slot><x-app-card><x-list-toolbar eyebrow="{{ __('Customers') }}" title="{{ __('Customer records') }}" :count="$customers->total()" :reset-url="route('reception.customers.index')" :active-filters="filled($search) ? 1 : 0" :collapsible="true"><form method="GET" class="flex gap-2"><input name="q" value="{{ $search }}" class="casa-input" placeholder="{{ __('Name, code, or phone') }}"><button class="casa-button-secondary">{{ __('Search') }}</button></form></x-list-toolbar><div class="mt-5">@if($customers->isEmpty())<x-empty-state title="{{ __('No customers found') }}" description="{{ __('Try another search.') }}"/>@else<x-table-shell label="{{ __('Reception customer records') }}"><thead class="bg-casa-bg text-left text-xs font-black uppercase tracking-[0.1em] text-casa-muted"><tr><th class="px-4 py-3">{{ __('Customer') }}</th><th class="px-4 py-3">{{ __('Phone') }}</th><th class="px-4 py-3">{{ __('Appointments') }}</th><th class="px-4 py-3">{{ __('Payments') }}</th><th class="px-4 py-3">{{ __('Action') }}</th></tr></thead><tbody class="divide-y divide-casa-border text-sm">@foreach($customers as $customer)<tr class="casa-table-row"><td class="px-4 py-4"><strong>{{ $customer->user?->name }}</strong><span class="block text-casa-muted">{{ $customer->customer_code }}</span></td><td class="px-4 py-4">{{ $customer->user?->phone ?: __('Not set') }}</td><td class="px-4 py-4">{{ $customer->appointments_count }}</td><td class="px-4 py-4">{{ $customer->transactions_count }}</td><td class="px-4 py-4"><a class="font-bold text-casa-primary" href="{{ route('reception.customers.show',$customer) }}">{{ __('Open') }}</a></td></tr>@endforeach</tbody></x-table-shell><div class="mt-5">{{ $customers->links() }}</div>@endif</div></x-app-card></x-app-layout>
